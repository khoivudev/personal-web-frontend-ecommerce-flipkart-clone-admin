import React from "react";
import Layout from "../../components/Layout";

const Orders = (props) => {
  return (
    <Layout sidebar>
      <div>
        <h3>Orders</h3>
      </div>
    </Layout>
  );
};

export default Orders;
